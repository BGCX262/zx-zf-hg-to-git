<?php
echo $this->content;

$this->star = '<sup>&nbsp;*&nbsp;</sup>';

if (!$this->done) {//if:

/*
	if (!empty($this->errors)) {
		foreach ($this->errors as $v) {
			echo $v . "<br/>";
		}
	}
	
	if ($this->mode == 'insert') {
		$this->submitlTitle = 'Зарегистрироваться';
	} else {
		$this->submitlTitle = 'Сохранить данные';
	}
*/

	echo $this->form;

}//endif: